<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.weishu.upf.demo.app2">


    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:ignore="GoogleAppIndexingWarning">
        <service android:name=".MyService" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActionActivity">
            <intent-filter>
                <action android:name=".ActionActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver android:name=".Receiver1">
            <intent-filter>
                <action android:name="com.weishu.upf.demo.app2.Receiver1" />
            </intent-filter>
        </receiver>

        <receiver android:name=".Receiver2">
            <intent-filter>
                <action android:name="com.weishu.upf.demo.app2.Receiver2" />
            </intent-filter>
        </receiver>

        <service android:name=".TargetService1" />
        <service android:name=".TargetService2" />
    </application>
</manifest>
